package com.example.admin4teacher.Interfaces;


import android.content.Context;

import persistencia.Classes;
import persistencia.Course;

public interface AuxiliarCourses {
    //si realmente necesitan que explique esto regresen a primer año plis :)
    void opcionEliminar(Course curso, Context context);
}
